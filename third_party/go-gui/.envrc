# Suppress "ignoring duplicate libraries: '-lobjc'" warnings on macOS.
# Multiple CGO packages each link Apple frameworks (AppKit, Cocoa, Metal,
# Foundation), and each transitively pulls -lobjc into the link step.
if [[ "$(uname)" == Darwin ]]; then
  export CGO_LDFLAGS="-Wl,-no_warn_duplicate_libraries"
fi
